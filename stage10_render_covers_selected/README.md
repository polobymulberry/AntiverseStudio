# Stage10 手工筛选封面

在 **`stage9_render_covers/`** 中挑选 **6** 张 **`<模型名>_cover.png`** 复制到本目录（正片以 **5** 套为目标，多 **1** 套备损；路径位于 `output/stage4_10/<模板>/<需求截断>/stage10_render_covers_selected/`）。

后续 **`--pass videos`** 会**仅**为本目录里出现过的 `*_cover.png` 所对应的 `stage8` 模型生成 **5 秒**环绕 **mp4**（见 `stage11_render_videos/`），未选中的 **GLB 不渲**正片，以省时间。白模视频见 **Stage12**。
